server:
  port: 8084

spring:
  application:
    name: SearchMS 
  cloud:
    consul:
      discovery:
        hostname: localhost 
      host: localhost 
      config:
        enabled: true
        fail-fast: false 
        prefix: config
        default-context: application
        data-key: data
        profile-separator: '::'
        format: yaml